You have a new reply from <%= @message.sender.profile.name %>
===============================================

RE: <%= @message.subject.html_safe? ? @message.subject : strip_tags(@message.subject) %>

-----------------------------------------------
<%= @message.body.html_safe? ? @message.body : strip_tags(@message.body) %>
-----------------------------------------------

Visit <%= root_url %> and go to messages to view your inbox